container_commands:
  01_npm_install:
    command: "npm ci --omit=dev"
    cwd: "/var/app/staging"

  02_rebuild_node_modules:
    command: "npm rebuild"
    cwd: "/var/app/staging"

  03_fix_permissions:
    command: "sudo chown -R node:node /var/app/staging/"
